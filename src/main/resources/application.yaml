spring:
  application:
    name: gestionusuarios

  datasource:
    url: jdbc:mysql://localhost:3306/eminencia
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 9090